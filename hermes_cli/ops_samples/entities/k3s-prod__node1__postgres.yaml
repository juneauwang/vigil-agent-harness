# postgres —— 第三层实体档案（docker compose 部署）。
name: postgres
detail: entities/k3s-prod__node1__postgres.yaml
version: "16"
updated_at: 2026-08-23
checks: []
snapshot:
  captured_at: "2026-08-23T00:00:00+00:00"
  source: manual
  common: {version: "16", config_dir: /etc/postgresql, log_dir: /var/log/postgresql, data_dir: /var/lib/postgresql, mode: single}
  by_type: {backup_dir: /backup/postgres, role: standalone}
  by_runtime:
    docker_compose:
      project: db
      workdir: /opt/db
      services: [{name: db-postgres-1, state: running}]
notes: ""
