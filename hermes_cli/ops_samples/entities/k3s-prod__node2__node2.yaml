# node2 —— 阿里云 k3s 集群工作节点（2026-08-08 实测部分信息）。
name: node2
type: k8s-node
env: prod
cluster: k3s-prod
role: worker
attrs:
  internal_ip: 203.0.113.14
  public_ip: 203.0.113.11
  spec: ecs.e-c1m2.xlarge
  k8s_version: v1.28.15
  disk: "cloud_essd_entry 40G (29G used, 73%)"
  # 连接方式见 topology.yaml hosts 行的 credential 引用（样例为占位符）
  maintenance_window: "周日 02:00-04:00（示例，待调整）"
  verified_via: terraform.tfstate
depends_on: []
depended_by: [k3s-prod]
ops:
  healthcheck: "kubectl get node node2"
