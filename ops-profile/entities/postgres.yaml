# postgres —— 生产数据库实例（样例值请按实际核对）。
name: postgres
type: db
env: prod
attrs:
  version: "16"                           # 样例值，请核对
  # 端口/凭据不落拓扑文件
depended_by: [harbor, order-db]
ops:
  healthcheck: "pg_isready"
