#!/usr/bin/env python3
"""
Argus CLI launcher (fork of Hermes Agent).

This wrapper should behave like the installed `argus` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from hermes_cli.main import main
    main()
